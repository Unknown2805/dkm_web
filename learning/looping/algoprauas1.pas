program algoprauas1;
uses crt;
var
a,b,c,d,e:integer;

begin
clrscr;

    // no 1
        // a :=5;
        // a := a + 1;
        // d := (14 + a * 2) div 2;
        // d := d - a;
        // b := 0;
        // c := 1;
        // while ( c <= d ) do 
        //     begin
        //         if(d mod 2 = 0) then
        //             begin
        //                 Inc(b);
        //             end;
        //         Inc(c);
        //     end;
        //
        // writeln(b);
        // writeln(d);
        // if( b = 2) then
        // begin    
        //     writeln('Universitas');
        // end
        // else
        //     begin
        //         writeln('Indraprasta');
        //     end;

    // no 2
        // a
            // a := 5;
            // b := 10;
            // while ( a > 0 ) do
            //     begin       
            //         b := b-a;
            //         a := a - 1;
            //     end;
            // write(b);
        
        // b
            // for a:=1 to 5 do 
            //     begin
            //         if( a mod 2 = 0) then
            //             writeln('merah')
            //         else 
            //             if( a mod 3 = 0) then
            //                 writeln('kuning')
            //             else
            //                 writeln('hijau');
            //     end;
    
    // no 3
        // a:= 1;
        // repeat
        //     if(a mod 3 = 0) or (a mod 5 = 0) then
        //         begin                
        //             write(a,'  ');
        //         end;
        //     Inc(a);
        // until (a > 500);
        
    // no 4
        // write('input jumlah angka perulangan maksimal: ');readln(d);
        // a:= 2;
        // c:= a + (d-1);
        // e:= a*d;
        // while a <= e do 
        //     begin
        //         b:=a;
        //         while b <= c do 
        //             begin
        //                 write(b,' ');
        //                 Inc(b);
        //             end;
        //         writeln;
        //         a := a + 2;
        //         c:= c+1;
        //     end;  
end.