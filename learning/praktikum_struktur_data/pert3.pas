program pert3;

// program 1
    // uses crt;
    // type tanggal = record
    // bulan, tanggal, tahun : integer;
    // end;
    // var waktu : tanggal;
    // begin

    // write('masukkan tanggal:  ');readln(waktu.tanggal);
    // write('masukkan bulan:  ');readln(waktu.bulan);
    // write('masukkan tahun:  ');readln(waktu.tahun);

    // // waktu.tanggal :=25;
    // // waktu.bulan:=09;
    // // waktu.tahun:= 1983;
    // writeln(' hari ini adalah tanggal  ',waktu.tanggal,':',waktu.bulan,':', waktu.tahun)
    // end.


// program 2
    // uses crt;
    // type tanggal = record
    // bulan, hari, tahun : integer;
    // end;
    // var waktu : tanggal;
    // begin {program utama}
    // with waktu do {mulai with}
    // begin

    // write('masukkan tanggal:  ');readln(waktu.tanggal);
    // write('masukkan bulan:  ');readln(waktu.bulan);
    // write('masukkan tahun:  ');readln(waktu.tahun);
    // // hari :=25;
    // // bulan:=09;
    // // tahun:=1983;
    // writeln('hari ini adalah ',hari,':',bulan,':', tahun)
    // end {akhir with}
    // end.